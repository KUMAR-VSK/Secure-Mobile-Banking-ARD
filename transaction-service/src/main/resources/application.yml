spring:
  application:
    name: transaction-service
  datasource:
    url: jdbc:mysql://localhost:3306/banking_transaction?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8083
